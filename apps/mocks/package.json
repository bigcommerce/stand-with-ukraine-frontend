{
  "name": "mocks",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "rm -rf tmp/ && mkdir -p tmp/ && cp -R src/* tmp/ && node tmp/app.mjs"
  },
  "dependencies": {
    "connect-api-mocker": "^1.10.0",
    "express": "^4.19.2"
  }
}
